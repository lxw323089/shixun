server:
  port: 8080

spring:
  application:
    name: employee-management

  datasource:
    url: jdbc:mysql://localhost:3306/worker_management?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 20050124

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect

jwt:
  secret: employee-management-system-secret-key
  expiration: 86400000
